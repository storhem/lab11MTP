[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "texttools"
version = "0.1.0"
description = "Python text processing extension written in Rust (PyO3)"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "texttools"
