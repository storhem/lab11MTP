# Prompt Log

## Задание М1: Dockerfile для Python-приложения (FastAPI)

### Промпт 1
**Инструмент:** Claude Code

"Давай начнем. Сделай Dockerfile для Python-приложения возьмём restnotes.py — это FastAPI-приложение с CRUD-эндпоинтами для заметок (GET/POST/DELETE /notes) из ЛР 6. К работе необходимы файлы README, PROMPT_LOG и .gitignore, который будет содержать ВСЕ файлы, которые не должны попасть в репозиторий."

Результат:
Claude изучил репозиторий ЛР6, получил содержимое restnotes.py и создал двухстадийный Dockerfile (builder → python:3.12-slim), requirements.txt, 9 unit-тестов, .gitignore, README.md и PROMPT_LOG.md. Все тесты проходят.

### Промпт 2
**Инструмент:** Claude Code

"Допиши еще тестов."

Результат:
Добавлены 10 новых тестов — итого 19. Покрыты граничные случаи: структура ответа, поле detail в ошибках 404, инкремент ID после удаления, длинный текст, спецсимволы. Все 19 тестов проходят.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Задание М7: Volume для обмена данными между контейнерами

### Промпт 1
**Инструмент:** Claude Code

"Теперь сделай следующее задание: Использовать volume для обмена данными между контейнерами. Делай максимальное покрытие тестами, где это уместно."

Результат:
Обновлён restnotes.py — добавлена персистентность через JSON-файл на shared volume (env NOTES_FILE). Создан notes-reader сервис с read-only доступом к тому же volume. Написан docker-compose.yml с named volume notes-data: notes-api монтирует его read-write, notes-reader — read-only. Тесты: 24 для notes-api (включая 5 тестов персистентности), 14 для notes-reader (включая 2 интеграционных теста обмена данными через volume). Итого 38 тестов, все проходят.

### Промпт 2
**Инструмент:** Claude Code

"Теперь проверь файлы readme, prompt и gitignore, ничего ли не упущено в них? Проверь еще раз коды и тесты."

Результат:
Найдены и исправлены: дублирование NOTES_FILE в reader.py, .dockerignore ошибочно находился в .gitignore (его нужно коммитить), отсутствовал .claude/ в .gitignore, в README не была указана папка reader/ и указано устаревшее число тестов. Все 38 тестов проходят после проверки.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин

---

## Задание М9: Ограничение ресурсов (CPU, память)

### Промпт 1
**Инструмент:** Claude Code

"Необходимо выполнить задание Ограничить ресурсы (CPU, память) для контейнеров. Сделай его, обеспечь максимальное покрытие тестами."

Результат:
В docker-compose.yml добавлен блок deploy.resources с limits и reservations для обоих сервисов: notes-api (0.50 CPU, 256M / резерв 0.25 CPU, 128M), notes-reader (0.25 CPU, 128M / резерв 0.10 CPU, 64M). Написаны 36 тестов в tests/test_compose_resources.py: валидация структуры файла, наличия всех блоков, корректности значений CPU и памяти, соотношения reservation ≤ limit, и логика (reader ≤ api). Все тесты проходят.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Задание Н1: Go-приложение в scratch-образе

### Промпт 1
**Инструмент:** Claude Code

"Собери Go-приложение с поддержкой статической компиляции и запусти в scratch образе. Также обеспечь максимальное покрытие тестами."

Результат:
Создан Go HTTP-сервис (GET /, /ping, /health, /info) на stdlib без внешних зависимостей. Двухстадийный Dockerfile: builder-стадия запускает тесты и компилирует бинарник с CGO_ENABLED=0, -ldflags="-s -w -extldflags '-static'"; финальная стадия — scratch с копированием CA-сертификатов. Исправлена ошибка: GET / при Go ServeMux является catch-all, поэтому изменено на GET /{$} для точного совпадения. Написаны 42 теста через httptest (табличные и индивидуальные): статус-коды, Content-Type, JSON-валидность, поля ответов, 405 для неверных методов, 404 для несуществующих маршрутов. Все тесты проходят.

### Промпт 2
**Инструмент:** Claude Code

"Проверь все еще раз. Проверь структуру проекта, тесты и листинги программ."

Результат:
Найдены и исправлены: отсутствие .benchmarks/ в .gitignore, неверное число тестов в README и PROMPT_LOG (было 44, стало 42). Все 74 Python-теста и 42 Go-теста проходят.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин

---

## Задание Н7: Multi-stage Python + Rust расширение

### Промпт 1
**Инструмент:** Claude Code

"Создай multi-stage сборку для Python-приложения с Rust-расширением."

Результат:
Создан Rust-модуль texttools (PyO3): word_count, reverse_string, is_palindrome, fibonacci. FastAPI-приложение использует модуль через эндпоинты /word-count, /reverse, /palindrome, /fibonacci. Двухстадийный Dockerfile: builder-стадия устанавливает Rust, собирает wheel через maturin, запускает 52 теста; финальная стадия — python:3.12-slim, только wheel и приложение. Написаны 52 теста: 31 юнит-тест Rust-функций напрямую, 21 тест FastAPI-эндпоинтов. Все тесты проходят.

### Промпт 2
**Инструмент:** Claude Code

"Теперь допиши еще тестов для последнего задания и выполни полную проверку всего проекта."

Результат:
Добавлены 31 новый тест: для texttools — unicode, смешанные пробелы, спецсимволы, инволюция reverse, чётные/нечётные палиндромы, строка только из пунктуации; для API — 422 при отсутствии обязательных параметров, Content-Type всех эндпоинтов, структура ответов, unicode, свойство последовательности Фибоначчи. Итого 83 теста для Н7. Полная проверка всего проекта: 157 Python-тестов и 42 Go-теста — все проходят.

### Промпт 3
**Инструмент:** Claude Code

"Сделай полный код ревью проекта, проверь на наличие ошибок, замечаний."

Результат:
Проведён полный код-ревью всех файлов проекта. Найдены и исправлены: 1) В lib.rs — is_palindrome использовала .map(|c| c.to_lowercase().next().unwrap()), что некорректно для Unicode-символов вроде ß (расширяется в "ss"). Исправлено на .flat_map(|c| c.to_lowercase()). 2) В python-rust/Dockerfile — добавлено копирование Cargo.lock для воспроизводимых сборок, зафиксирована версия maturin==1.7.4, финальная стадия теперь копирует только app/main.py без тестов. 3) В python/Dockerfile и reader/Dockerfile — добавлены кавычки к ENV PATH="/root/.local/bin:$PATH" (без кавычек PATH интерпретируется как строка, а не переменная окружения). 4) Созданы .dockerignore файлы для всех четырёх сервисов (python, reader, go, python-rust) — исключают тесты, кэш, логи из контекста сборки.

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~35 мин
