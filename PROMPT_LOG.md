# Prompt Log — Лабораторная работа №11

## Задание М1: Dockerfile для Python-приложения (FastAPI)

### Промпт 1
**Инструмент:** Claude Code

**Промпт:** "Давай начнем. Сделай Dockerfile для Python-приложения возьмём restnotes.py — это FastAPI-приложение с CRUD-эндпоинтами для заметок (GET/POST/DELETE /notes) из ЛР 6. К работе необходимы файлы README, PROMPT_LOG и .gitignore, который будет содержать ВСЕ файлы, которые не должны попасть в репозиторий."

**Результат:** Claude изучил репозиторий ЛР6, получил содержимое restnotes.py. Создал:
- `python/restnotes.py` — скопирован из ЛР6
- `python/requirements.txt` — fastapi, uvicorn, httpx, pytest
- `python/Dockerfile` — двухстадийная сборка (builder → slim)
- `python/tests/test_notes.py` — 9 unit-тестов на все эндпоинты
- `.gitignore` — для Python, Go, Rust, Docker, IDE, ОС
- `README.md` — описание проекта и инструкции
- `PROMPT_LOG.md` — этот файл

**Что было решено автоматически:**
- Healthcheck без curl (используется `urllib.request` из стандартной библиотеки Python)
- Зависимости копируются через `/root/.local` для чистоты финального образа
- Тесты сбрасывают состояние через `autouse` фикстуру, чтобы не зависеть от порядка запуска

### Промпт 2
**Инструмент:** Claude Code

**Промпт:** "Допиши еще тестов"

**Результат:** Добавлены 10 новых тестов (итого 19):
- Проверка структуры ответа (`id`, `text`)
- Проверка поля `detail` в ошибках 404
- ID продолжает инкрементироваться после удаления
- Получение каждой из нескольких заметок по ID
- Удаление одной заметки из нескольких — остальные сохранены
- GET удалённой заметки возвращает 404
- Заметка с длинным текстом (1000 символов)
- Заметка со спецсимволами

### Итого по М1
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Все 19 тестов проходят

---

## Задание М7: Volume для обмена данными между контейнерами

_Будет заполнено при выполнении задания._

---

## Задание М9: Ограничение ресурсов (CPU, память)

_Будет заполнено при выполнении задания._

---

## Задание Н1: Go-приложение в scratch-образе

_Будет заполнено при выполнении задания._

---

## Задание Н7: Multi-stage Python + Rust расширение

_Будет заполнено при выполнении задания._
